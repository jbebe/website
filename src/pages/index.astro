---
import { getCollection } from 'astro:content';
import Layout from '../components/Layout.astro';

const { Content: About } = await (await getCollection('about'))[0].render();
---

<Layout>
  <About />
</Layout>
